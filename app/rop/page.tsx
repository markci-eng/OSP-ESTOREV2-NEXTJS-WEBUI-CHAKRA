"use client";
import { Container } from "@chakra-ui/react";
import { RopPage } from "osp-chakra-reusable-components"
export default function Page() {
    return (
        <Container maxW={"6xl"} mx={"auto"} py={150}>
            <RopPage onClick={() => {}}/>
        </Container>
    )
}